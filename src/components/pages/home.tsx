import { useAppSelector } from "../../store/hooks";
import Board from "../shared/board";

const Home = () => {
  // console.log(data);
  return (
    <div>
      <Board />
    </div>
  );
};

export default Home;
